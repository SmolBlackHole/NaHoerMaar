{
	"name": "nahormaar",
	"version": "0.1.0",
	"description": "Private Discord music bot with a web dashboard.",
	"private": true,
	"type": "module",
	"license": "MPL-2.0",
	"engines": {
		"node": ">=24.11.0"
	},
	"workspaces": [
		"frontend"
	],
	"scripts": {
		"licenses": "node scripts/licenses.mjs",
		"dev": "npm run dev --workspace frontend",
		"build": "npm run build --workspace frontend",
		"check": "npm run check --workspaces",
		"test": "npm run test --workspaces"
	},
	"overrides": {
		"esbuild": "^0.28.2"
	},
	"devDependencies": {
		"@types/node": "^26.6.2",
		"fontless": "0.2.1",
		"license-checker-rseidelsohn": "5.0.1"
	}
}
